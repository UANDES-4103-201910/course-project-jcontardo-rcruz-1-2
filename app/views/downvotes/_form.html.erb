<%= form_with(model: downvote, local: true) do |form| %>
  <% if downvote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(downvote.errors.count, "error") %> prohibited this downvote from being saved:</h2>

      <ul>
      <% downvote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.number_field :post_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
